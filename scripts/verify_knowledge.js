/**
 * 64 卦知识完整性校验 —— 确保 100% 正确
 * 校验每一卦：卦序、卦名、上下卦、六爻卦象、与传世本《周易》一致
 */

// 项目里的查表（与 constants.ts 保持一致）
const HEXAGRAM_LOOKUP = [
  [    1, 10, 13, 25, 44,  6, 33, 12], // 上=乾
  [   43, 58, 49, 17, 28, 47, 31, 45], // 上=兑
  [   14, 38, 30, 21, 50, 64, 56, 35], // 上=离
  [   34, 54, 55, 51, 32, 40, 62, 16], // 上=震
  [    9, 61, 37, 42, 57, 59, 53, 20], // 上=巽
  [    5, 60, 63,  3, 48, 29, 39,  8], // 上=坎
  [   26, 41, 22, 27, 18,  4, 52, 23], // 上=艮
  [   11, 19, 36, 24, 46,  7, 15,  2], // 上=坤
];
const TRIGRAM_YAO = {
  1: [1, 1, 1], // 乾 [初,中,上]
  2: [1, 1, 0], // 兑
  3: [1, 0, 1], // 离
  4: [1, 0, 0], // 震
  5: [0, 1, 1], // 巽
  6: [0, 1, 0], // 坎
  7: [0, 0, 1], // 艮
  8: [0, 0, 0], // 坤
};
const TRIGRAM_NAMES = { 1: '乾', 2: '兑', 3: '离', 4: '震', 5: '巽', 6: '坎', 7: '艮', 8: '坤' };

// 传世本《周易》64 卦标准六爻（用 ䷀ 等 Unicode 卦符的二进制定义）
// Unicode 卦符从 U+4DC0(乾) 开始，但排序不是周易序，所以我们用权威的"卦名→六爻"对照
// 六爻用字符串表示，从初爻到上爻，1=阳 0=阴
const STANDARD = {
  1: ['乾', '111111'], 2: ['坤', '000000'], 3: ['屯', '100010'], 4: ['蒙', '010001'],
  5: ['需', '111010'], 6: ['讼', '010111'], 7: ['师', '010000'], 8: ['比', '000010'],
  9: ['小畜', '111011'], 10: ['履', '110111'], 11: ['泰', '111000'], 12: ['否', '000111'],
  13: ['同人', '101111'], 14: ['大有', '111101'], 15: ['谦', '001000'], 16: ['豫', '000100'],
  17: ['随', '100110'], 18: ['蛊', '011001'], 19: ['临', '110000'], 20: ['观', '000011'],
  21: ['噬嗑', '100101'], 22: ['贲', '101001'], 23: ['剥', '000001'], 24: ['复', '100000'],
  25: ['无妄', '100111'], 26: ['大畜', '111001'], 27: ['颐', '100001'], 28: ['大过', '011110'],
  29: ['坎', '010010'], 30: ['离', '101101'], 31: ['咸', '001110'], 32: ['恒', '011100'],
  33: ['遁', '001111'], 34: ['大壮', '111100'], 35: ['晋', '000101'], 36: ['明夷', '101000'],
  37: ['家人', '101011'], 38: ['睽', '110101'], 39: ['蹇', '001010'], 40: ['解', '010100'],
  41: ['损', '110001'], 42: ['益', '100011'], 43: ['夬', '111110'], 44: ['姤', '011111'],
  45: ['萃', '000110'], 46: ['升', '011000'], 47: ['困', '010110'], 48: ['井', '011010'],
  49: ['革', '101110'], 50: ['鼎', '011101'], 51: ['震', '100100'], 52: ['艮', '001001'],
  53: ['渐', '001011'], 54: ['归妹', '110100'], 55: ['丰', '101100'], 56: ['旅', '001101'],
  57: ['巽', '011011'], 58: ['兑', '110110'], 59: ['涣', '010011'], 60: ['节', '110010'],
  61: ['中孚', '110011'], 62: ['小过', '001100'], 63: ['既济', '101010'], 64: ['未济', '010101'],
};
// 注：上面六爻串是「初爻→上爻」，例如屯卦 100010 = 初阳 二阴 三阴 四阴 五阳 上阴
//     屯 = 坎上震下：下卦震[1,0,0]，上卦坎[0,1,0] → 100 010 ✓

// 由六爻串反推上下卦
function yaoToTrigram(yao3) {
  for (const [k, v] of Object.entries(TRIGRAM_YAO)) {
    if (v.join('') === yao3) return parseInt(k);
  }
  return null;
}

let errors = [];
let ok = 0;

for (let num = 1; num <= 64; num++) {
  const [name, yao] = STANDARD[num];
  const lowerYao = yao.slice(0, 3);  // 初二三 = 下卦
  const upperYao = yao.slice(3, 6);  // 四五上 = 上卦
  const lower = yaoToTrigram(lowerYao);
  const upper = yaoToTrigram(upperYao);

  // 用我们的查表验证：上下卦组合应该得到正确卦序
  const lookupResult = HEXAGRAM_LOOKUP[upper - 1][lower - 1];

  if (lookupResult !== num) {
    errors.push(`卦${num}(${name}): 上${TRIGRAM_NAMES[upper]}下${TRIGRAM_NAMES[lower]} 查表得 ${lookupResult}，应为 ${num}`);
  } else {
    ok++;
  }
}

console.log(`\n========== 64卦知识校验 ==========`);
console.log(`✓ 正确: ${ok}/64`);
if (errors.length > 0) {
  console.log(`✗ 错误: ${errors.length}`);
  errors.forEach(e => console.log('  ' + e));
} else {
  console.log(`\n🎉 全部 64 卦的上下卦组合与传世本《周易》100% 一致！`);
}
